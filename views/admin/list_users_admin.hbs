{{> burger_menu}}
{{> current_user}}

<div id="team-manage">
    <h2>Manage all the users</h2>
    <table>
        <thead>
            <tr>
                <th></th>
                <th></th>
                <th>Name</th>
                <th>Email</th>
                <th>Role</th>
                <th>Date Add</th>
                <th>Date Update</th>
                <th>Edit</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            {{#each users}}
            <tr>
                <td>
                    <img width="50px" src="{{this.avatar}}" alt="{{this.firstname}}">
                </td>
                <td>
                    <img width="50px" src="{{this.company.logo}}" alt="{{this.company.name}}">
                </td>
                <td>
                    <p>{{this.firstname}} {{this.lastname}}</p>
                </td>
                <td>
                    <p>{{this.email}}</p>
                </td>
                <td>
                    <p>{{this.role}}</p>
                </td>
                <td>
                    <p>{{formatDate this.createdAt}}</p>
                </td>
                <td>
                    <p>{{formatDate this.updatedAt}}</p>
                </td>
                <td><a class="fas fa-edit" href="/dashboard/settings/admin/users/edit/{{this._id}}"></a></td>
                <td><a class="fas fa-trash" href="/dashboard/settings/admin/users/delete/{{this._id}}"></a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</div>

<div id="manage-btn">
    <a href="/dashboard/settings/admin/users/create/"><i class="fas fa-plus"></i> Add a new user</a>
</div>