{{> burger_menu}}
{{> current_user}}

<div id="team-manage">
    <h2>Manage all the companies</h2>
    <p>Last five companies added to brelio</p>
    <table>
        <thead>
            <tr>
                <th></th>
                <th>Name</th>
                <th>Phone Number</th>
                <th>Date Add</th>
                <th>Date Update</th>
                <th>Edit</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            {{#each companies}}
            <tr>
                <td>
                    <img width="50px" src="{{this.logo}}" alt="{{this.name}}">
                </td>
                <td>
                    <p>{{this.name}}</p>
                </td>
                <td>
                    <p>{{this.phonenumber}}</p>
                </td>
                <td>
                    <p>{{formatDate this.createdAt}}</p>
                </td>
                <td>
                    <p>{{formatDate this.updatedAt}}</p>
                </td>
                <td><a class="fas fa-edit" href="/dashboard/register/{{this._id}}"></a></td>
                <td><a class="fas fa-trash" href="/companies/delete/{{this._id}}"></a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    <div id="manage-btn">
        <a href="/companies/create"><i class="fas fa-plus"></i> Add a new company to brelio</a>
        <a href="/companies/all"><i class="fas fa-user-cog"></i> Manage all the companies</a>
    </div>

</div>