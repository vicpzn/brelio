{{> burger_menu}}
{{> current_user}}

<div id="company-manage">
    <h2>Your company</h2>
    <div id="your-company"><img width="150px" src="{{currentCompany.0.logo}}" alt="{{currentCompany.0.name}}">
        <div>
            <h3>{{currentCompany.0.name}}</h3>
            <address>{{currentCompany.0.streetnumber}} {{currentCompany.0.street}}, {{currentCompany.0.zipcode}},
                {{currentCompany.0.city}}, {{currentCompany.0.country}}</address>
            <p>{{currentCompany.0.phonenumber}}</p>
        </div>
    </div>

    <a href="/dashboard/register/{{currentCompany.0._id}}"><i class="fas fa-building"></i> Edit company</a>
</div>

<div id="team-manage">
    <h2>Manage your team</h2>
    <p>Last five users added</p>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Phone Number</th>
                <th>Role</th>
                <th>Date Add</th>
                <th>Date Update</th>
                <th>Edit</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            {{#each membersTeam}}
            <tr>
                <td>
                    <p>{{this.firstname}} {{this.lastname}}</p>
                </td>
                <td>
                    <p>{{this.phonenumber}}</p>
                </td>
                <td>
                    <p>{{this.role}}</p>
                </td>
                <td>
                    <p>{{formatDate this.createdAt}}</p>
                </td>
                <td>
                    <p>{{formatDate this.updatedAt}}</p>
                </td>
                <td><a class="fas fa-edit" href="/users/edit/{{this._id}}"></a></td>
                <td><a class="fas fa-trash" href="/users/delete/{{this._id}}"></a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    <div id="manage-btn">
        <a href="/users/create"><i class="fas fa-plus"></i> Add a new user to your team</a>
        <a href="/users/all"><i class="fas fa-user-cog"></i> Click here to manage all the users</a>
    </div>

</div>